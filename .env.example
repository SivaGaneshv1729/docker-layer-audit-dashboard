# Port the dashboard will be accessible on
DASHBOARD_PORT=3000

# Port the analyzer API will be accessible on
API_PORT=8000

# Docker Socket Path (Default for Linux/macOS, Windows uses pipes but Docker Desktop usually maps this)
# DOCKER_SOCKET=/var/run/docker.sock
