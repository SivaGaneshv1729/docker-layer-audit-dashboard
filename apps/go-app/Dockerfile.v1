FROM golang:1.19

WORKDIR /app

# Bad practice: Copies everything, including potential secrets or temp files
COPY . .

# Installs dependencies after copying the whole project, which hurts caching
RUN go mod tidy

# Builds the application
RUN go build -o /app/main .

EXPOSE 8080
CMD ["/app/main"]
