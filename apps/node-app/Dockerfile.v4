# Minimalist Node.js image
FROM node:18-alpine
WORKDIR /usr/src/app

# Optimized: Copy package files and install production deps only
COPY package*.json ./
RUN npm ci --only=production && npm cache clean --force

COPY index.js ./

# Security: Non-root user
USER node

EXPOSE 3000
CMD [ "node", "index.js" ]
